import React from 'react';
import './styles.css';

const AppBar = (props) => {
  return (
    <footer class="bar app-footer">
        <button>Cart</button>
        <button>Scan</button>
        <button>Profile</button>
    </footer>
  );
};

export default AppBar;